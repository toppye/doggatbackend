
Spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/mydb1
    username: MUIZ
    password: aremucoding
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql

